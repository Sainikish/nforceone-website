# Project skills

Drop project-scoped skills here, one folder per skill:

```
.claude/skills/<skill-name>/SKILL.md
```

`SKILL.md` needs YAML frontmatter with `name` and `description`, followed by the
skill's instructions in Markdown. These skills are only available in this
project (as opposed to `~/.claude/skills/`, which are available everywhere).
